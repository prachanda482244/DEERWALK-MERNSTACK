export function isGreaterThan18(age) {
    if (age >= 18) {
        console.log(`This user age is ${age}`);
    } else {
        console.log(`This user age is less than 18 actual age :- ${age}`);
    }
}